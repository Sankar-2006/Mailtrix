const http = require('http'); // http library which you need.

function getLogin(err, token) {
    const userResp = ''; // url
    console.log(userResp);

    if (userResp === true) {
        // user functionalities
    } else {
        throw(err)
    }
}

function saveTokenHeaders(token) {
    getLogin.userResp['token'];
}